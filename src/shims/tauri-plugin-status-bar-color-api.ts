/**
 * Client-side shim for `tauri-plugin-status-bar-color-api`.
 * Only used in the web dev server — the real module is available on mobile.
 */
export async function setStatusBarColor(_color: string) { }
