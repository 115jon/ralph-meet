/**
 * Client-side shim for `@tauri-apps/plugin-process`.
 * Only used in the web dev server — the real module is available on desktop.
 */
export async function relaunch() { }
export async function exit() { }
