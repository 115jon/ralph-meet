# Generated by Cargo
# will have compiled files and executables
/target/
# Isolated Cargo target dir used by test runs (CARGO_TARGET_DIR=target-test) so a
# running desktop app holding a lock on /target/debug does not block test builds.
/target-test/
/gen/schemas
